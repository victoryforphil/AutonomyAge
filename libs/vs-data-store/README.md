# vs-data-store

The Victory Suite datastore. Typed, bucketed primitives with a topic/key model,
retention, serialization, and a listener for external access.

Part of the [Victory Suite](../) workspace (under `libs/`).
